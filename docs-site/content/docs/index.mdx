---
title: Getting Started
description: Overview and quickstart for aba-payway-sdk-unofficial
---

The unofficial, edge-ready TypeScript SDK for integrating ABA Bank PayWay payment gateway in Cambodia.

## Features

- **Zero Node Dependencies**: Built on Web Crypto API (`crypto.subtle`) — works across Node 18+, Deno, Bun, and Cloudflare Workers.
- **Purchase Creation**: Generate ABA Checkout payment links and deep links with automatic parameter hashing.
- **Transaction Status**: Poll and verify transaction states with typed status mapping.
- **Secure Webhook Verification**: Constant-time signature comparison to prevent timing attacks.
- **KHQR SVG Card**: Generates responsive, standalone SVG QR payment cards.

---

## Installation

Install the package from NPM:

```bash
npm install aba-payway-sdk-unofficial
```

Or using Yarn, PNPM, or Bun:

```bash
pnpm add aba-payway-sdk-unofficial
```

---

## Quickstart

```typescript
import {
  ABAPayWay,
  generateTransactionId,
} from "aba-payway-sdk-unofficial";

// 1. Initialize client
const aba = new ABAPayWay({
  merchantId: process.env.ABA_MERCHANT_ID!,
  apiKey: process.env.ABA_API_KEY!,
  baseUrl: "https://checkout-sandbox.payway.com.kh",
});

// 2. Create a purchase
const transactionId = generateTransactionId();

const purchase = await aba.createPurchase({
  transactionId,
  amount: 10.0,
  currency: "USD",
  items: "Standard Subscription",
  firstName: "Dara",
  lastName: "Sok",
  email: "dara@example.com",
  returnUrl: "https://yoursite.com/payment/success",
  cancelUrl: "https://yoursite.com/payment/cancel",
});

if (purchase.success) {
  console.log("PayWay Web Checkout:", purchase.checkoutUrl);
  console.log("ABA Mobile App Deeplink:", purchase.abapayDeeplink);
  console.log("Raw EMV QR Data:", purchase.qrString);
}
```
